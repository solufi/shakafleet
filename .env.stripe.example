# Stripe Configuration (Fleet Manager)
# Add these to your .env.local or environment
#
# The Fleet Manager only handles webhook forwarding to RPi.
# All Stripe API calls are made directly from the RPi agent.

# Stripe Webhook Signing Secret (starts with whsec_)
# Found in Stripe Dashboard > Developers > Webhooks > Signing secret
# Used to verify incoming webhook signatures
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXX
