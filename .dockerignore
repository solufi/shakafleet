node_modules
.next
.git
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local deploy files are still kept in repo, but not needed in image context
/deploy

# RPi agent files (not part of the Fleet Manager build)
/agent
