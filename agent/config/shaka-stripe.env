# Stripe Terminal Configuration
# Server-driven integration with WisePOS E reader
# Docs: https://docs.stripe.com/terminal
#
# The agent calls Stripe API directly from the RPi.
# Webhooks are received by Fleet Manager and forwarded to RPi.

# Stripe Secret Key (starts with sk_test_ for sandbox, sk_live_ for production)
# Found in Stripe Dashboard > Developers > API keys
STRIPE_SECRET_KEY=

# Stripe Reader ID (assigned when registering the WisePOS E)
# Found in Stripe Dashboard > Terminal > Readers (e.g. tmr_xxx)
STRIPE_READER_ID=

# Machine ID for this RPi
MACHINE_ID=default

# Simulation mode: 1=enabled (no real API calls), 0=disabled (real Stripe)
STRIPE_SIMULATION=1

# Decimal places for price (Canada=2, meaning 100 = $1.00)
STRIPE_DECIMAL_PLACES=2

# Vend result timeout in seconds
STRIPE_VEND_RESULT_TIMEOUT=30

# API request timeout in seconds (RPi → Fleet Manager)
STRIPE_API_TIMEOUT=15

# Pre-auth max amount in cents for multi-vend ($50.00)
STRIPE_PREAUTH_MAX_AMOUNT=5000

# State file (shared with vend server)
STRIPE_STATE_FILE=/tmp/shaka_stripe_state.json

# Simulation settings
STRIPE_SIM_APPROVAL_DELAY=3.0
STRIPE_SIM_AUTO_APPROVE=1
